NAME
    AO3 Downloader

SYNOPSIS
    ao3-download -u UID [options]

OPTIONS
    -uid        User ID on AO3. [required]

    -processes  Processes to run at once.

    -format     Format to download works in. Valid values are epub
                (default), mobi, pdf, and html.

    -directory  Where to download files (default current directory).

    -section    Section to download. Valid values are bookmarks (default),
                and works. (Collections and Serieses are not supported at
                this time).

DESCRIPTION
    This program will download the works found for a section of a given user
    page.

